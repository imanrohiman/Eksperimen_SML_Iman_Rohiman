name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.8'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install mlflow scikit-learn pandas numpy joblib
    
    - name: Run MLflow Project
      run: |
        cd MLProject
        mlflow run . --env-manager=local
    
    - name: Build and Push Docker image
      run: |
        docker build -t nuelahadl/workflow-ci:latest .
        echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u nuelahadl --password-stdin
        docker push nuelahadl/workflow-ci:latest
